from Configure import conftest
from TaskGen import feature, extension, before, after
import Options

sources = [
]

pyste_module = "libtrilinos"
pyste_sources = [
	"src/pyste/wrapper.pyste",
]

def set_options(opt):
	pass

def configure(conf):
	print("-----> Configuring trilinos")


def build(bld):
	if bld.is_module_enabled("trilinos"):
		tskgen = bld.new_task_gen(
			features="pyprop pyste compile link",
			source=sources,
			local_libraries="core/libcore",
			pyste_files=pyste_sources,
			pyste_module=pyste_module,
			pyste_force=False,
			pyprop_path="../../../../",
		)

